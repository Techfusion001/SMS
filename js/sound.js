const SoundFX=(()=>{const key="sentinel_sound_enabled";let enabled=localStorage.getItem(key)!=="off";let ctx=null;function ensureCtx(){if(!ctx)ctx=new(window.AudioContext||window.webkitAudioContext)();return ctx}function beep(type="nav"){if(!enabled)return;try{const audio=ensureCtx();const osc=audio.createOscillator();const gain=audio.createGain();const now=audio.currentTime;const map={nav:[520,.035,.025],confirm:[880,.055,.035],danger:[160,.11,.045]};const[f,l,v]=map[type]||map.nav;osc.type="sine";osc.frequency.setValueAtTime(f,now);gain.gain.setValueAtTime(v,now);gain.gain.exponentialRampToValueAtTime(.0001,now+l);osc.connect(gain);gain.connect(audio.destination);osc.start(now);osc.stop(now+l)}catch(e){}}function toggle(){enabled=!enabled;localStorage.setItem(key,enabled?"on":"off");updateButtons();beep(enabled?"confirm":"danger")}function updateButtons(){document.querySelectorAll("#soundToggle,#settingsSoundToggle").forEach(btn=>{if(btn)btn.textContent=enabled?"Sound: On":"Sound: Off"})}document.addEventListener("click",e=>{const el=e.target.closest("[data-sound]");if(el)beep(el.dataset.sound)});document.addEventListener("DOMContentLoaded",updateButtons);return{beep,toggle,updateButtons,isEnabled:()=>enabled}})();
